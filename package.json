{
  "name": "denon-firebase",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint src",
    "serve": "nodemon -r babel-register -r config-variables src",
    "start": "node -r babel-register -r config-variables src"
  },
  "license": "MIT",
  "dependencies": {
    "@unfold/babel-preset": "^1.0.3",
    "babel-cli": "^6.23.0",
    "config-variables": "^1.1.0",
    "firebase": "^3.6.10",
    "split2": "^2.1.1"
  },
  "devDependencies": {
    "@unfold/eslint-config": "^2.0.1",
    "eslint": "^3.15.0"
  }
}
